<div id="side">
    <li>
        <a href="/signin">회원 가입</a>
    </li>
    
  
    <% if( locals.user.displayName ){ %>  <!-- locals. 를 꼭 접두사로 붙혀야 언디파인드도 체크 됨 -->
        <div style="text-align:center">
            이미 로그인 되어 있습니다. <br>
            <a href="/logout">로그아웃</a>
        </div>
     <% }else{ %>
        <div >
            <form method="post" action="/auth/login">
                <table style="height:80px">
                    <tr style="padding-left:10%"> 
                        <td style="width:20%">
                            <input style="height:50%" type="text" name="id" placeholder="아이디">
                            <input style="height:50%" type="password" name="password" placeholder="비밀번호">
                        </td>
                        <td style="width:40%">
                            <input class="btn-1" style="width:100%; height:100% "type="submit" value="로그인">
                        </td>
                    </tr>
                </table>
            </form>
        </div>
    <% } %>
</div>