<!DOCTYPE HTML>
<html>
    <head>
        <meta charset="utf-8">
        <title><%=title%></title> 
    </head>

    <link rel='stylesheet' href="/custom.css">

    <body>
            <% include ./menubar.ejs %>
    <div id="root"> 
        

        <% include ./leftsidebar.ejs %>

        <div id="con">
            <div id="con2" >
        <H1 style=""><%=title%></H1> 
        <button class="btn-1" style="width:150px; height:50px; float:right"onclick="location.href='/board/writeform'">글쓰기</button> <br>
        <br><br>
        <Hr style="margin-top:30px">
        
            <table id="table" style="margin-bottom:10px; border-bottom:1px solid black; width:100%; height:20%">
                <tr style="background:#E6E6E6">
                    <td style="text-align:center; ">번호</td>
                    <td style="text-align:center; ">제목</td>
                    <td style="text-align:center; ">조회수</td>
                    <td style="text-align:center; ">글쓴이</td>
                    <td style="text-align:center; ">날짜</td>
                    
                </tr>
            <% for ( var i =boards.length - (page*20-20) -1 ; i > boards.length-(page*20) ; i--) { %>
                <% if ( i < 0 ) { break; } %>
                <tr>
                    <td style="text-align:center; width:5%;"> <%=boards[i].idx%> </td>
                    <td style="width:60%"> <a href="/board/boardview?id=<%=boards[i].idx %>" style=""><%=boards[i].title %></a> </td>
                    <td style="text-align:center;">  <%=boards[i].view %> </td>
                    <% if ( boards[i].isFormal == 1 ) { %>
                        <td style="text-align:center;">  <strong><%=boards[i].name %></strong> </td>
                    <% }else{ %>
                        <td style="text-align:center;">  <%=boards[i].name %> </td>
                    <% } %>
                    <td style="text-align:center;"> <%=boards[i].date%> </td>
                    
                </tr>
                <% } %>
            </table>

            <div class="listnumcon">
            <% for ( var i = 1 ; i < boards.length/20+1 ; i++ ) { %>
             <a class="listnum" style="" href="/board?page=<%=i%>"><%=i%></a>  
            <% } %>
             </div>   
        </div>
    </div>
</div>
        <% include ./footer.ejs %>
   
    </body>
</html>